name: Debian Headless VM

on:
  workflow_dispatch:

jobs:
  debian:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Debian VM
        uses: vmactions/debian-vm@v1
        with:
          mem: 4096
          debug-on-error: true
          run: |
            echo "🔧 تثبيت SSH..."
            apt-get update -qq
            apt-get install -y -qq openssh-server nano curl

            echo "🔑 إعداد مفتاح SSH..."
            mkdir -p /root/.ssh
            chmod 700 /root/.ssh
            echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKabR0ItQG3t0UFr9gk0QlL3OzkAtrfgNLkWJ9y4CWZk youssefhidour@youssefhidour-hpprobook640g1" > /root/.ssh/authorized_keys
            chmod 600 /root/.ssh/authorized_keys

            echo "⚙️ تفعيل root login..."
            echo 'PermitRootLogin yes' >> /etc/ssh/sshd_config
            echo 'PasswordAuthentication yes' >> /etc/ssh/sshd_config
            echo 'root:debian' | chpasswd

            echo "🚀 تشغيل SSH..."
            /usr/sbin/sshd

            echo "🌐 فتح نفق Pinggy..."
            ssh -p 443 -o StrictHostKeyChecking=no -o ServerAliveInterval=30 -R0:localhost:22 tcp@free.pinggy.io

            exit 1
